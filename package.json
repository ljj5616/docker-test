{
  "name": "simple-notepad",
  "version": "1.0.0",
  "private": true,
  "description": "컨테이너 없이 실행하는 간단한 Node.js 메모장",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check public/app.js"
  }
}
